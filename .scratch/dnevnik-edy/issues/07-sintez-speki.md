# 07 — Синтез спеки дневника → spec.md

Type: task
Status: resolved
HITL/AFK: HITL

Resolved: выполнен навыком `to-spec` — синтезирована [spec.md](../spec.md) (`ready-for-agent`), сводящая решения 01–06 + разбор реального кода в build-ready спеку. Это и есть пункт назначения карты.

Blocked by: 01, 02, 03, 04, 05, 06

## Question

Терминальный тикет карты — пункт назначения. Собрать все закрытые решения (тикеты 01–06) в единую **build-ready спеку** `.scratch/dnevnik-edy/spec.md`, готовую к передаче в `writing-plans`, по образцу `.scratch/racion-mvp/spec.md`.

Спека должна включать:
- **Проблема и цель** (килфича «ответ на дельту», не просто дельта).
- **Модель данных** — `DiaryEntry`, `Ingredient.gramsPerPiece`, типы `/core/diary` (из тикета 02).
- **Чистое ядро** — `amount` / `aggregate` / `progress` / **`suggest`**: контракты, формулы, зафиксированный алгоритм скоринга по отстающему макросу (из 02/03/04).
- **Actions/queries** — контракт, идемпотентность, таймзона, guard (из 05).
- **Экраны** — экран дня с блоком «что поесть сейчас», поток добавления (из 04/06); ссылка на прототип-ассет.
- **Крайние случаи и тестовые швы** — сведённый список (штуки без грамм-эквивалента, двойное списание, таймзона, перебор, поздний старт, снапшот-неизменяемость, пустые состояния) + Vitest-швы ядра и action-уровня.
- **Границы** — Out of scope из карты; порядок стройки (модель+миграция → ядро с тестами → actions/queries → экран дня → поток добавления → полировка/пустые состояния).

Результат: `spec.md` со статусом `ready-for-agent`. После этого карта пройдена — решать перед стройкой нечего.

Работа тикета: свести, проверить непротиворечивость решений между тикетами, дописать `CONTEXT.md`, если остались незафиксированные термины. Это `task` (сведение), а не новое решение.
