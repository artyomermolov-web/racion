# 11: Иконки-действия (HIG SVG) + раскрытие рецепта по тапу

**What to build:** Действия приёма в ленте дня становятся иконками своего инлайн-SVG набора в стиле Apple HIG (вместо текстовых подписей), а тап по блюду разворачивает инлайн-аккордеон с рецептом. Лента становится чище и быстрее на телефоне, а рецепт виден по месту без ухода на отдельный экран.

**Blocked by:** 09.

**Status:** done

- [x] Свой набор инлайн-SVG иконок (без внешних зависимостей, как `TabBar.tsx`), сетка 24×24, тап-таргет ≥ 44pt: **съел** (галочка-в-круге; состояния off/on/busy), **заменить** (круговые стрелки), **удалить** (корзина), **добавить** (плюс). — `src/components/ios/ActionIcon.tsx`, `.icon-btn` (44×44).
- [x] Иконки-только, но с сохранёнными `aria-label` (доступные имена уже есть в коде); «съел» отражает три состояния через `aria-pressed`/busy. — toggle съесть/снять с `aria-pressed`+`aria-busy`.
- [x] «Перегенерировать» остаётся текстовой кнопкой (первичное действие дня).
- [x] Тап по строке приёма разворачивает инлайн-аккордеон: рецепт — состав/шаги/время; продукт — компактная сводка КБЖУ/порции. Повторный тап сворачивает; раскрыт один за раз.
- [x] Тап по строке (раскрытие) и тап по иконке-действию — разные зоны; действие не срабатывает при раскрытии, и наоборот. — тело-`button` и иконки — соседи, не вложены.
- [x] Данные рецепта берутся по `refId` из слоя еды (`RecipeDetail`), при необходимости ленивым догрузом. — `getRecipeDetailAction` (`src/app/actions/food.ts`) + кэш по recipeId.
- [x] Прогон в браузере: иконки читаемы в свету/тьме, раскрытие/сворачивание работает, доступность иконок сохранена. — проверено на seed-данных; съеденная строка рендерится корректной карточкой (не ручной записью), консоль без ошибок.
