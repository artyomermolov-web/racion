# 05 — Навигация: Дневник главный, «Меню» упраздняется

Type: task
Status: resolved (свёрнут в spec.md через to-spec)
Blocked by: 01

## Question

Зафиксировать целевую навигацию (решения из чартинга уже приняты — здесь довести до спеко-готовности):

1. **Вкладки:** Дневник (первая, стартовая) · База · Покупки · Профиль. Вкладка «Меню» (`/home`) убирается из `TabBar.tsx`.
2. **Стартовый маршрут:** после входа → `/dnevnik` (сейчас, вероятно, `/home`). Проверить редиректы `src/app/page.tsx`, `(app)/layout.tsx`, логин.
3. **Судьба `/home`:** удалить маршрут или оставить redirect → `/dnevnik`? Что со старыми ссылками/PWA-ярлыком.
4. **Куда девается содержимое `/home`:** операции плана вливаются в ленту дня (тикет 01/04); «Неделя» — отдельный экран (тикет 06). Убедиться, что ничего не теряется (проверить `HomeContent.tsx`, `DiaryHomeRow.tsx`).

Резолв: точная карта маршрутов/редиректов и перечень «что переезжает / что удаляется» — в ответе тикета. Питает синтез спеки (07).
