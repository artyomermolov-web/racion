# 10: Навигация — Дневник главный, «Меню» упраздняется

**What to build:** Дневник становится первой и стартовой вкладкой; вкладка «Меню» убирается из таб-бара, её маршрут `/home` редиректит в `/dnevnik`. Ничего не теряется — все операции плана уже живут в ленте дня (09), недельный план откроется отдельным экраном (13).

**Blocked by:** 09 (Дневник должен уметь всё, что умело «Меню», до её удаления).

**Status:** ready-for-human

- [x] Таб-бар: **Дневник (первый) · База · Покупки · Профиль**; вкладка «Меню» удалена.
- [x] После входа приложение открывается на `/dnevnik` (проверить редиректы `src/app/page.tsx`, `(app)/layout.tsx`, логин).
- [x] Открытие `/home` (старая ссылка/PWA-ярлык) редиректит на `/dnevnik`.
- [x] Содержимое `/home` не осиротело: `DiaryHomeRow`/`HomeContent` убраны или переиспользованы без потери функций; недельный план не выброшен (открывается в 13, до 13 — доступен как раньше или помечен «скоро»).
- [x] Прогон в браузере: чистая навигация, старт на Дневнике, старые ссылки не 404-ят.

## Comments

Реализовано (Opus 4.8):

- Таб-бар `TabBar.tsx`: убрана вкладка «Меню» (`/home`), Дневник — первый. Осталось 4 вкладки.
- Стартовые редиректы → `/dnevnik`: `src/app/page.tsx`, `login/page.tsx`, `register/page.tsx`, `actions/auth.ts` (login+register). `(app)/layout.tsx` править не потребовалось — там только `requireUser()`, хардкода `/home` не было.
- `/home` → серверный `redirect("/dnevnik")` (файл маршрута сохранён для старых ссылок и PWA-ярлыка через `start_url:"/"`).
- `HomeContent.tsx` и `DiaryHomeRow.tsx` удалены — их функции живут в ленте `DayFeed` (тикет 09).
- **Недельный план — ветка «доступен как раньше»**: создан маршрут `/dnevnik/nedelya`, переиспользующий `WeekPlan` как есть (недельное среднее, переключение дней, «Заменить», перегенерация). Вход — иконка-календарь в шапке Дневника. Тикет 13 добавит иконки-действия (из 11) и переход день недели → лента этого дня.
- `revalidatePath("/home")` заменён на `/dnevnik/nedelya` в `customFood`/`preferences`/`profile` (там ревалидировалась серверная генерация плана — теперь она на экране недели).
- Рефактор по код-ревью: seed недели вынесен в общий `currentWeekSeed()` в `@/lib/generator` и переиспользован экраном недели и списком покупок (устранён дубль формулы).
- Проверки: `npm run typecheck` ✓, `npm run build` ✓ (17 маршрутов), `npm test` ✓ (361), браузерный прогон ✓ (старт на Дневнике, `/home`→`/dnevnik`, экран недели, консоль чистая).
- Оставлен мёртвый `src/components/DayPlan.tsx` (единственный импортёр `HomeContent` удалён) — заведена отдельная задача на его удаление, вне скоупа навигационного тикета.
