# 03: Частичная смета в списке покупок

**What to build:** Список покупок считает стоимость по реальным ценам ВкусВилл; позиции без мэтча исключаются из суммы и помечаются, а вся смета — как приблизительная.

**Blocked by:** 02

**Status:** ready-for-agent

Опора: [spec](../spec.md) (Q9=c), `CONTEXT.md` (Частичная смета), шов `src/core/shopping/list.test.ts`.

- [ ] `core/shopping/list.ts`: позиции с `source!="vkusvill"` вне `lineCost`/`totalCost`, но остаются в списке и в КБЖУ/меню; флаг `partial` + счётчик исключённых.
- [ ] Тест `list.test.ts`: смета с дырами → корректный total без исключённых + `partial=true`; без дыр → `partial=false`.
- [ ] UI списка покупок: бейдж «приблизительно» + пометка позиций без цены.
- [ ] Снапшот покупки продолжает фиксировать цену на момент подтверждения (не сломать).
- [ ] Демо: список покупок с реальной суммой и честной пометкой пробелов.
