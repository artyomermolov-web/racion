# 16: Вкусы — избранное, блок, recurring, фильтры

**What to build:** Пользователь отмечает блюда как избранное, блокирует нежелательные, задаёт recurring (often/always), добавляет фильтры «не хочу есть» — и генератор это учитывает: заблокированное никогда не появляется, фильтр ловит синонимы, избранное предлагается чаще, recurring попадает регулярно/обязательно.

**Blocked by:** 15

**Status:** ready-for-human

Опирается на решения: 06. Наполняет предпочтения (Favorite/Block/RecurringFood/KeywordFilter), которые генератор уже умеет принимать.

- [x] Действия из карточки блюда/приёма: избранное, блок, recurring often/always.
- [x] Экран фильтров «не хочу есть» (по группам и синонимам).
- [x] Режим типа приёма «только recurring».
- [x] Тесты: блок никогда не просачивается; keyword-фильтр ловит все словоформы (напр. «курица» → грудка/бедро/фарш); recurring-always присутствует; избранное даёт бонус.

## Comments

Реализовано в коммите c35ec14. Ядро `/core/generator`: `Preferences` (favorite/often/always) со скоринговым бонусом (often > favorite), forced-выбор always-recurring с защитой от вытеснения отжигом, режим слота `onlyRecurring`, синонимы keyword-фильтра (`keywords.ts`). Модель: Favorite/Blocked/Recurring/KeywordFilter/MealSlotSetting (+ миграция). UI: действия на карточке блюда + экран `/profil/vkusy`. Тесты «на утечки» — в `preferences.test.ts` и `keywords.test.ts` (все 166 тестов зелёные, прод-сборка проходит).

Осознанное отступление от decision 06: числовые веса бонусов (favorite=0.15, often=0.3, W_PREF=1) отмасштабированы под нормированное дневное отклонение, а не буквальные 2/3 из доски (доска помечает веса «тюнингуемыми»; относительный порядок often > favorite сохранён).
