# 21: PWA-офлайн, полное наполнение, полировка, README

**What to build:** Пользователь устанавливает приложение на домашний экран и офлайн видит последние планы. База наполнена полностью (~211 продуктов, 60–80 рецептов). Интерфейс отполирован: пустые состояния с подсказками, понятные ошибки на русском, всё управляется одной рукой. Есть README на русском.

**Blocked by:** 20

**Status:** ready-for-human

Опирается на решения: 01, 10.

- [x] Service worker кэширует оболочку и последние планы; запросы к API — network-only; установка на домашний экран. (`public/sw.js` v2: network-first навигация с runtime-кэшем последних страниц-планов; проверено — runtime-кэш содержит `/home`.)
- [x] Полный сид: 211 продуктов (из `research/products-ru.md`) + 63 собственных рецепта; правдоподобность КБЖУ под тестом (`catalog.test.ts` + `recipe.test.ts`). `npm run seed` → 211/63.
- [x] Пустые состояния с подсказкой «что сделать»: добавлен CTA «Заполнить профиль» на главной. Русские доступные ошибки (`role=alert`/`aria-invalid` в формах входа/профиля) и mobile-first «одной рукой» реализованы ранее (тикеты 11/12/16) — здесь проверены, без изменений в этом дифе.
- [x] README на русском: запуск одной командой + деплой на VPS (`Dockerfile` + `docker-compose.yml` с PostgreSQL, раздел «Деплой на VPS»).

Заметка: вердикт недели `compromised` приведён к мягкой трактовке клетчатки
(`FIBER_SOFT_TOLERANCE`) — согласовано с целевой функцией отжига; убирает ложное
предупреждение при 1–2 г недобора клетчатки и идеальных макросах на полной базе.
